local chatService = game:GetService("TextChatService")

if chatService and chatService:FindFirstChild("ChatWindowConfiguration") then
    chatService.ChatWindowConfiguration.Enabled = true
else
    warn("cheat was not found")
end
